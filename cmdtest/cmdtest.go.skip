package main

import (
	"log"
	"os"
	"syscall"
)

func main() {
	//err := syscall.Exec("/bin/ls", []string{"ls", "-al"}, os.Environ())
	err := syscall.Exec("/sbin/ping", []string{"/sbin/ping", "www.google.com"}, os.Environ())
	log.Println(err)
}
